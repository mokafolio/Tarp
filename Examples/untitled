

typedef void * tpHandle;

typedef struct
{
	int index;
	int type;
} _tpGLHandle;

_tpGLHandle * handle = (_tpGLHandle *)_path;
if(handle->type == _kPath)
{
	_tpGLPath * path = &((_tpGLPath)_ctx->paths[handle->index]);
}
